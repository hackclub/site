name: CI
on:
  - push
  - pull_request

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run lint
